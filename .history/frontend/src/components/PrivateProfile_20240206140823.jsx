import {Button} from "react-bootstrap";

const PrivateProfile = ({name, userName}) => {
  return (
    <div
      style={{
        marginTop: "6rem",
        textAlign: "center",
        display: "flex",

        gap: "3rem",
      }}
    >
      <User userName={`${`} isLoggedIn={true} name={user.name} />
      <Button>Follow</Button>
    </div>
  );
};

export default PrivateProfile;
